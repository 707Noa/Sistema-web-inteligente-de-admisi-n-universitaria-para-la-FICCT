# CUP-FICCT — Documentación inicial

Este documento contiene instrucciones rápidas para arrancar el monorepo.

- Levantar servicios: `docker compose up --build`
- Backend: composer install, php artisan migrate
- Frontend: `npm install` y `npm run dev` (si se ejecuta fuera de Docker)
